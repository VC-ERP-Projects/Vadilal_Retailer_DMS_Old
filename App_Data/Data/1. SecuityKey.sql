DECLARE @PARENTID DECIMAL = 0
DECLARE @MAC NVARCHAR(50) = ''
DECLARE @DATE NVARCHAR(50) = ''

IF (@PARENTID > 0)
BEGIN
	UPDATE OEMP
	SET PASSWORD2 = @MAC, PASSWORD3 = @MAC, PASSWORD4 = @DATE
	WHERE PARENTID = @PARENTID
END
